# 测试说明

`cases.tsv` 保存必须长期成立的输入行为，依次记录输出模式、输入码、首选候选和测试目的。当前先用于人工回归；后续增加基于 librime API 的自动测试程序，逐条校验首选候选。

执行 `make check` 会验证 YAML、安装脚本、品牌配置、版本号和仓库卫生，并对 v0.5.0 通用学习模式执行一次隔离安装清单检查。

发布前还需在临时 Rime 用户目录完成真实部署，并人工验证：只有一个可工作的输入源、菜单栏圆角框“万”图标、横向候选、五笔优先、拼音补充、默认简体、通用联想、个人候选优先、关闭学习开关和重启后仍可使用。
