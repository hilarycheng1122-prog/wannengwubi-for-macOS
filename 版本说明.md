# 万能五笔版本说明

本目录是万能五笔唯一的主项目目录。当前源码、安装包构建工具、Git 历史和本地旧版本归档现已统一放在这里管理。

## 目录关系

```text
WanNengWubi/
├── VERSION                         # 当前开发版本号
├── CHANGELOG.md                    # 正式变更记录
├── docs/releases/                  # v1.0.0 之后的发布说明
├── dist/                           # 本机构建的安装包，不提交 Git
├── archives/                       # 本地旧版本快照，不提交 Git
│   └── WanNengWubi-Version-Pack/
│       ├── versions/               # v0.x 完整离线快照
│       ├── shared-installers/      # 旧版共用鼠须管安装包
│       └── checksums/              # 旧归档校验值
└── .git/                           # Git 版本历史和标签
```

## 当前正式版本

当前正式版本是 `v1.0.1`，也是建议普通用户安装的版本。

- 一体化 macOS `.pkg` 安装包，不需要打开终端。
- 五笔 86 与拼音混合输入，五笔候选优先。
- 默认统一输出简体，可用 `Control + Shift + T` 切换繁体。
- 包含通用下一词联想和完全离线的个人搭配学习。
- 粘贴和应用外部文本变化不会触发联想或个人学习。
- 安装前自动备份已有 Rime 配置，并保留个人学习数据库。

当前安装包位于 `dist/WanNengWubi-v1.0.1.pkg`。正式对外版本以 GitHub Release 和 Git 标签 `v1.0.1` 为准。

## 历史版本

| 版本 | 主要区别 | 保存方式 |
|---|---|---|
| `v0.1.1` | 五笔 86 + 拼音混输、五笔优先、横向候选、简体输出 | `archives/WanNengWubi-Version-Pack/versions/v0.1.1/` 与 Git 标签 |
| `v0.2.1` | 增加简繁切换，修复候选混入繁体字和菜单栏图标 | `archives/WanNengWubi-Version-Pack/versions/v0.2.1/` 与 Git 标签 |
| `v0.3.0-alpha.2` | 增加离线静态下一词联想 | `archives/WanNengWubi-Version-Pack/versions/v0.3.0-alpha.2/` 与 Git 标签 |
| `v0.4.0-alpha.1` | 增加离线个人搭配学习 | `archives/WanNengWubi-Version-Pack/versions/v0.4.0-alpha.1/` 与 Git 标签 |
| `v1.0.0` | 合并通用联想与个人学习，成为首个正式版本 | Git 标签及 `docs/releases/v1.0.0.md` |
| `v1.0.1` | 增加普通用户可双击安装的一体化安装包 | Git 标签及 `docs/releases/v1.0.1.md` |

旧归档子目录中的 `VERSION` 是当时源码内部版本字段，可能与最终 Git 标签名称不同；识别历史版本时应以子目录名和 Git 标签为准。

## 以后如何维护

- 日常开发只在本目录进行，不直接修改 `archives/` 中的旧快照。
- 当前稳定源码以 `main` 分支为准。
- 每次发布使用 Git 标签和 GitHub Release 保存，不再复制新的完整源码文件夹。
- `archives/` 仅用于离线查看和应急回退，已加入 `.gitignore`，不会造成 GitHub 仓库重复膨胀。
- 个人输入历史和个人学习数据库不进入 Git，也不放入版本归档。
